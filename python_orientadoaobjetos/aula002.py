"""usamos o comando #class Sth: como uma receita para construir o objeto
   normalmente definimos o nome do objeto com a primeira letra em maiusculo
   para construir o objeto usamos #NomeDoObjeto() e, isso nos fornece um endereço
   quardamos o endereço em uma varíavel (referência)
"""


class Account:

    pass

print(Account())

account = Account()
print(account)
